node_modules/
dist/
bin/
*.tsbuildinfo

# Transient help-dump artifact — `tu help-dump` emits the contract JSON; shll.ai
# pulls it via its own cron (shll.ai is the source of truth). `npm run help-dump`
# writes this locally for inspection; never committed here. See scripts/help-dump.mjs.
help/

.fab-status.yaml

.fab-*


.status.yaml.lock

!fab/.fab-version

.uploads/

/.claude/settings.local.json
