---
description: "Command layer: request grammar and flag parsing, guard normalization, the Run pipeline and Result, multi-mode gathering, and the cmd/tu entry point."
---
# Command Documentation

> **Generated by `fab docs-index`** — do not hand-edit; only the manual block below is preserved.

<!-- fab docs-index:manual:start -->
<!-- Hand-managed rows (non-markdown descriptions, external links, groupings) — preserved verbatim. -->
<!-- fab docs-index:manual:end -->

| File | Description |
|------|-------------|
| [entry-point](entry-point.md) | cmd/tu/main.go — the only writer of the process streams; routing order (Parse → version → non-data dispatch → HOME → config.Load → MetricsDirGuard → reserved-user check → deps → --sync block → watch branch → Run), the setup commands, the sync and watch branches, and the exit-code mapping |
| [guards](guards.md) | internal/command guards — Normalize's warn-and-clear flag guards (since/until off history, -u in single mode, --top off-leaderboard, --by-machine on the pivot/lbh, --full off history), the 3-month cap and --full, the reserved `all` user, and the leaderboard multi-mode gate |
| [multi-mode](multi-mode.md) | Multi-mode record composition — command.gather's four record paths by mode and -u, the own-user write-then-MaxMerge path, the repo-only -u paths, the mode-keyed snapshot label rule, and the auto-clone guard's placement in the data path |
| [request-and-parse](request-and-parse.md) | internal/command request model and parser — Request, Flags, the Display/Format/Metric enums, Parse's fixed-order flag pass and validation, UsageError/ShortUsage/FullHelp, and the 0/1/2 exit-code constants |
| [run-and-result](run-and-result.md) | command.Run and its result contract — the Deps seams (Fetcher/Repo/Writer/Now/Colors/Width/Live/LastSync), the in-scope check and ErrUnported, the Result fields (Lines/Notices/Warnings/TotalCost/TotalTokens/CostByItem), and the per-poll LiveOptions watch inputs |
