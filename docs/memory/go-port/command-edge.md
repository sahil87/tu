---
type: memory
description: The Go port's command edge — internal/command (Request, byte-exact Parse, ShortUsage/FullHelp, Normalize's guards + 3-month cap, Run(cfg) composing snapshot/history and the lb/lbh leaderboards into a Result, Deps.Live carrying per-poll watch render options, the Repo/Fetcher/Writer seams, ErrUnported for --skip-brew-update only) and cmd/tu as the only writer incl. the -w watch branch (watch-mode); sync via metrics-sync; config: config-and-setup; toolkit: toolkit-layer — 452/452 harness green
---
# Command Edge and Entry Point (Go port)

**Domain**: go-port

## Overview

`internal/config` owns the full config cascade and the setup commands ([config-and-setup](/go-port/config-and-setup.md)); `internal/command` parses the complete tu argument grammar into a `Request` and composes the pipeline — the input layer ([fact-and-sources](/go-port/fact-and-sources.md)) through the middle layers ([query-view-render](/go-port/query-view-render.md)) — into a `Result`; `cmd/tu` is the only writer. For fetching, `command` imports `source` and `fact` and no `source/*` adapter; `cmd/tu` is the only package that names `ccusage` and `metrics`. The binary answers the snapshot and history grammars in single AND multi mode (with `--since`/`--until`/`--full`, the `--by-machine` machine/user columns, the table, JSON, CSV and Markdown formats, and `-u <user>` / `-u all` against the metrics repo — the composition is [multi-mode](/go-port/multi-mode.md)), the leaderboards `lb`/`lbh` in multi mode (repo-only user ranking with previous-window deltas, `--top`, `--by-machine` on `lb`, all four formats) (2gbb), the setup commands `init-conf`, `init-metrics`, and `status`, the sync surfaces `tu sync`, `tu sync --dry-run` and `--sync` on a data command ([metrics-sync](/go-port/metrics-sync.md)) (lsml), and the toolkit surfaces `help`/`-h`/`--help`, `help-dump`, `skill`, `shell-init` and `update` ([toolkit-layer](/go-port/toolkit-layer.md)) for real; watch mode (`-w` on every display type, with `--no-rain`/`--interval` silently accepted without it — DC-03) runs the live TUI loop ([watch-mode](/go-port/watch-mode.md)) (4pze); the one remaining recognized-but-unported request (`--skip-brew-update` on a data command) keeps the scaffold's placeholder.

## Requirements

### Requirement: Config cascade
`internal/config` owns the full `tu.conf`/`org.conf`/legacy cascade — path resolution from `$HOME` only, the embedded shipped defaults, `Load(p, env, ov) (Config, []string)` merging defaults ∪ org ∪ user ∪ env ∪ CLI overrides and returning the cascade warning lines, and the setup commands. See [config-and-setup](/go-port/config-and-setup.md).

### Requirement: Request, Flags, UsageError, ShortUsage, FullHelp
`command` defines `Display` (`Snapshot`/`History`/`Leaderboard`/`LeaderboardHistory`), `Format` (`Table`/`JSON`/`CSV`/`Markdown`), `Metric` (`Cost`/`Tokens`), `Flags` (the booleans `Fresh, NoColor, Watch, Sync, DryRun, ByMachine, Full, NoRain, SkipBrewUpdate`; `Interval` defaulting to 10 and validated only when `Watch`; `User`/`Since`/`Until`; `Metric`; `Top` with 0 = unset), and `Request{Source, Period, Display, Format, Flags, Command, Args, Version}`. `Source` is `""` for all tools, else a registry key with aliases resolved; `Command` holds the first positional when it is a non-data command; `Args` carries the positionals after `Command` (nil for data commands) (4fs0). `command` exports the exit-code constants `ExitOK = 0`, `ExitOperational = 1`, `ExitUsage = 2` — the shll toolkit convention: 0 = success (including benign no-ops and warn-and-continue guards), 1 = operational failure (a well-formed invocation that could not complete; also the placeholder), 2 = usage error; `cmd/tu` returns only these values (4fs0). `UsageError{Message, ShowUsage}` means exit 2 with the message on stderr; `ShowUsage` appends `ShortUsage`, the byte-exact TS constant:

```
Usage: tu [source] [period] [display]

  tu                Today's cost, all tools
  tu cc             Today's cost, Claude Code
  tu mh             Monthly cost history, all tools
  tu -h             Show full help

Run 'tu help' for all commands.
```

`FullHelp` sits beside it in `request.go` — the byte-exact TS `FULL_HELP` constant (a Go raw string) that `tu help`/`-h`/`--help` print plus one newline; it is grammar documentation like `ShortUsage`, so `command` owns it while `internal/toolkit` receives it as a parameter (see [toolkit-layer](/go-port/toolkit-layer.md)).

### Requirement: Parse implements the complete TS grammar
`Parse(args) (Request, *UsageError)` is `parseGlobalFlags` + the version/non-data checks + `parseDataArgs`, in the TS order, so the same argv produces the same first error:

1. **Flag pass** over args in order. Sixteen boolean flags are stripped: `--json`, `-j`, `--csv`, `--md`, `--sync`, `--dry-run`, `--fresh`, `-f`, `--watch`, `-w`, `--no-color`, `--no-rain`, `--by-machine`, `--full`, `--skip-brew-update`, `-t`. Value flags consume the next token only when it qualifies — `^\d+$` for `--interval`/`-i`; a token not starting with `-` for `--user`/`-u`, `--since`/`-s`, `--until`, `--metric`, `--top` — and the flag is remembered as present either way. Everything else lands in the positional list, including `--help` when it is not the first token and unknown flags like `--bogus`.
2. **Validation, in this order, first failure wins** (message on stderr, exit 2, no usage block): `--interval` only when `--watch` — missing/non-numeric `Error: --interval requires a numeric value`; `< 5` `Error: --interval minimum is 5 seconds`; `> 3600` `Error: --interval maximum is 3600 seconds`. Then the six format conflicts in order — watch+json, json+csv, json+md, csv+md, watch+csv, watch+md — each `Error: {a} and {b} are incompatible` (`-j` counts as `--json`; the message always says `--json`). Then `-u` present without value: `Error: -u requires a username`. Then `--since`/`--until` shape — `YYYY-MM-DD` or `YYYYMMDD`, the compact form normalized to dashed — else `Error: --since requires a date (YYYY-MM-DD or YYYYMMDD)` (and the `--until` twin); shape-only, so `2026-13-01` parses successfully. Then both present and `since > until`: `Error: --since must be on or before --until`. Then `--metric` not `tokens`/`cost`: `Error: --metric requires 'tokens' or 'cost'`. Then `-t` with an explicit `--metric cost`: `Error: -t and --metric cost are incompatible` (`-t` alone or with `--metric tokens` sets `Metric = Tokens`). Then `--top` not a positive integer: `Error: --top requires a positive integer`. Format precedence is json > csv > md > table.
3. **Version after validation**: `--version`, `-V` or `-v` anywhere in the original args sets `Request.Version` — `tu --json --csv --version` is exit 2, not a version line.
4. **Help first**: a first positional in {`help`, `-h`, `--help`} sets `Request.Command` and returns — the help check precedes the `--dry-run` guard, so `tu help --dry-run` parses as help (4fs0).
5. **`--dry-run` misuse guard**: when `Flags.DryRun` and the first positional is not `sync` (including no positional), the error is `UsageError{Message: "Error: --dry-run is supported only with 'tu sync' — run 'tu sync --dry-run' to preview a sync.", ShowUsage: false}` — exit 2 with no usage block. `tu sync --dry-run` parses to `Command == "sync"` with `DryRun` set and no error (4fs0).
6. **Non-data commands**: a first positional in {`init-conf`, `init-metrics`, `sync`, `status`, `update`, `shell-init`, `help-dump`, `skill`} sets `Request.Command`, and the positionals after it become `Args` (nil when none). Only for `init-metrics`: `len(Args) > 1` → `UsageError{Message: "Error: init-metrics takes at most one argument (repo-url)", ShowUsage: true}` — message line, then `ShortUsage`, exit 2, raised before `$HOME` is consulted. Data flags on a setup command do not error: `status --json` sets both `Command` and `Format` (4fs0).
7. **Positionals**: a first token in `cc codex co oc gemini gem copilot cop kimi ki all` is the source (aliases `co→codex`, `gem→gemini`, `cop→copilot`, `ki→kimi`; `all` → `""`). Each remaining token: `d`/`daily`, `w`/`weekly`, `m`/`monthly` set Period; `h`/`history`, `lb`, `lbh` set Display; `dh`/`wh`/`mh` set Period and History together. Anything else — a second source, a source after a period, an unknown word or flag, `--help` after a positional — is `Unknown argument: {tok}` with `ShowUsage: true` (message line, then `ShortUsage`), exit 2.

#### Scenario: Byte-exact first errors
- **GIVEN** argv `--json --csv`, `-t --metric cost`, `cc codex`, `cc --help`, `--bogus`
- **WHEN** parsed
- **THEN** the messages are `Error: --json and --csv are incompatible`, `Error: -t and --metric cost are incompatible`, `Unknown argument: codex`, `Unknown argument: --help`, `Unknown argument: --bogus` (the last three with `ShowUsage`)

#### Scenario: Guard order — version and help win
- **GIVEN** argv `--dry-run --version`, `help --dry-run`, `sync --dry-run`, `init-metrics a b`, `status --json`
- **WHEN** parsed
- **THEN** the first sets `Version`; the second sets `Command == "help"`; the third sets `Command == "sync"` with `DryRun` and no error; the fourth is the arity usage error with `ShowUsage`; the fifth sets `Command == "status"` and `Format == JSON` with no error

### Requirement: --interval without --watch is parse-inert
A bare `--interval N`/`-i N` without `--watch` is dropped at parse level: the numeric value is consumed and discarded, validation is watch-gated, and the snapshot renders normally — exactly as the TS behaves (the harness cases `interval-unsupported` and `interval-without-watch-long` are green). A non-numeric value is NOT consumed and lands in the positional list, so `tu --interval abc` (no `--watch`) is `Unknown argument: abc`, exit 2. (3am6)

### Requirement: Normalize applies the warn-and-clear guards and the cap
`Normalize(req, mode, now) (Request, []string, capActive bool)` in `guards.go` is the pure guard step — it takes the post-guard `config.Mode` and returns the request the pipeline runs plus the stderr notice lines the edge prints BEFORE any fetch warning. In TS order:

0. `User` set in single mode on a display outside {`lb`, `lbh`} → notice `Warning: -u flag requires multi mode — ignoring.`; `Flags.User` cleared. The notice applies to `-u all` exactly as to any name. The leaderboards are exempt so the exit-1 `ErrLeaderboardMode` gate — which runs in `Run` BEFORE `Normalize` — fires without a notice line; in multi mode this step never runs (2gbb).
0'. `User == "all"` on `lb`/`lbh` → cleared silently, no notice (the leaderboard is inherently all-users). `-u <name>` is kept: it PINS that user's row (the ` ◂` marker), never filters (2gbb).
0a. `ByMachine` on the all-tools history pivot (`Source == "" && Display == History`) → notice `Warning: --by-machine is not supported with all-tools history — ignoring.`; `ByMachine` cleared. The single-tool history and the snapshots keep the flag (pmsd).
0b. `ByMachine` on the leaderboard history → notice `Warning: --by-machine is not supported with leaderboard history — ignoring.`; `ByMachine` cleared. `--by-machine` on `lb` is kept — it keys rows by `user/machine` (pmsd, 2gbb).
1. `Since`/`Until` set on a display outside {`h`, `lb`, `lbh`} → notice `Warning: --since/--until apply to history display — ignoring.`; both cleared (so the snapshot is in scope after the clear and a well-shaped impossible date like `2026-13-01` warns-and-renders with exit 0 — harness case `since-invalid`). The leaderboards keep the window; on `lb` it REPLACES the period window (2gbb). The `-u` notice precedes this one when both fire.
2. `Full` on a display outside {`h`, `lbh`} → notice `Warning: --full applies to daily/weekly history — ignoring.` (the flag is left set; nothing reads it downstream). `lb --full` warns like a snapshot; `lbh` does not (2gbb).
2b. `Top` set on a display outside {`lb`, `lbh`} → notice `Warning: --top applies to leaderboard display — ignoring.`; `Top` cleared. Positioned after the `--full` notice and before the cap — the TS order — so a bare `tu --top 3` warns and renders the snapshot, exit 0 (DC-03) (2gbb).
3. **Cap**: display ∈ {`h`, `lbh`} ∧ period ≠ monthly ∧ `Since == "" && Until == ""` ∧ `!Full` → `Since = query.ThreeMonthFloor(now)`, `capActive = true`. An explicit bound on either side disables the cap entirely (no intersection); `mh --full` is a silent no-op; `--full` with an explicit window is silently accepted; `lb` is never capped (2gbb). The defaulted floor windows repo-sourced records identically — `Window` runs on the collapsed daily records after the merge ([multi-mode](/go-port/multi-mode.md)).

(3am6, 9ax5, pmsd)

### Requirement: Run composes the in-scope request, else ErrUnported
In scope (evaluated on the **normalized** request): `Display ∈ {Snapshot, History, Leaderboard, LeaderboardHistory}`; `Format` Table, JSON, CSV or Markdown; `Source` any tool or all; any period; single or multi mode (the leaderboards never reach this check in single mode — the `ErrLeaderboardMode` gate fires first); `-u` in any mode (Normalize already cleared it for single, and `-u all` for the leaderboards); flags limited to `--json`/`-j`, `--csv`, `--md`, `--fresh`/`-f`, `--no-color`, `-t`, `--metric`, `--since`/`-s`, `--until`, `--full`, `--top` (Normalize already cleared it off the leaderboards), and `--by-machine` on the snapshots, the single-tool history and `lb` (Normalize already cleared it on the all-tools pivot and on `lbh`); `--watch`/`-w`, `--no-rain` and `--interval` (the edge runs the watch loop before `Run`; a watch request that reaches `Run` renders the ordinary one-shot table, as do bare `--no-rain`/`--interval` — DC-03 silent acceptance) (4pze); no non-data command; not `Version`. Anything else — `DryRun`, `SkipBrewUpdate` — returns `ErrUnported` without fetching and without printing the notices (the TS prints them and then does the unported thing; no harness case combines them). `Sync` is admitted: the edge consumes `--sync` before `Run`, so the flag never reaches the scope check with work to do (lsml). `Run(ctx, req, cfg config.Config, deps Deps)` takes the post-guard config: it needs `Mode` (the record path, the leaderboard gate, and the snapshot label rule) plus `User` and `Machine` (the own/other-machine split, and the leaderboard's default pinned user). Before `Normalize`, the leaderboard gate runs: `leaderboard(req.Display) && cfg.Mode == config.Single` → `Result{}, ErrLeaderboardMode` (2gbb — the full contract is the leaderboard requirement below). For an in-scope request:

1. Normalizes: `req, notices, capActive := Normalize(req, cfg.Mode, deps.Now())`; then the scope check; then `context.WithTimeout(ctx, source.DefaultTimeout)` once, for all tools.
2. Gathers the daily records through `gather(ctx, req, cfg, deps)` — the four record paths of [multi-mode](/go-port/multi-mode.md): single → the live fetch; multi own-user → the own-user day-file write through `Deps.Writer` (per tool in registry order, when wired) **before** the read, then the live fetch reconciled with the stored day-files via `MaxMerge` plus the other machines' records; `-u all` / `-u <other>` → repo-only, no fetch, no source errors, no writes. `gather` returns the stamped, **un-collapsed** records in a fixed, load-bearing order (own-machine `MaxMerge(live, own)` output first, then other machines in walk order — year asc, machine asc, file asc; for `-u all`, users ascending then walk order); the snapshot/history tails apply `query.Collapse(recs, query.Tool, query.Date)` themselves so the main-table bytes are unchanged, while the `--by-machine` breakdown groups the same raw records on the machine/user dimension the collapse would drop (pmsd). The fetch itself is **daily only** (`source.PeriodDaily`, no extra args — roll-up is client-side): `FetchAll` for all tools, `Fetch(tool)` for a single source, with `fresh = Flags.Fresh`. `Fetcher` is the live seam — its `Fetch` takes `fact.Tool`; `*ccusage.Source` satisfies it, asserted at the `cmd/tu` assignment. `Repo` (`Users()` + `Read(user, tool)`) is the metrics-clone seam, consulted only in multi mode; `metrics.Source` satisfies it, asserted likewise.
3. **Snapshot** — collapses `Collapse(raw, Tool, Date)` for the main table, computes `cur = query.CurrentLabel(Period, deps.Now())` and groups `GroupBy(Window(RollUp(recs, Period), cur, cur), Tool)`; builds `[]view.ToolTotals` over registry order (all six tools, or the one source) with `Name` from `fact.Lookup` and `Label = cur` when a group matched, else `""`; clears every `Label` only when `cfg.Mode == config.Single && Source == "" && Period == Daily && !Flags.ByMachine` — the TS single-mode daily-all path (`fetchAllTotals`) returns bare totals, so `tu --json` never carries `"label"` in single mode while `tu cc --json` (any period), `tu m --json` / `tu w --json`, and every multi-mode snapshot with a current-label record do (3am6); under `--by-machine` the TS path (`fetchToolMergedWithMachines`) yields labelled entries even in single mode, so the clear does not apply (pmsd). When `Flags.ByMachine`, `buildSnapshotBreakdown` composes the machine/user column set from the un-collapsed records: the dimension is `query.Machine` with legend noun `Machines`, or `query.User`/`Users` when `Mode == Multi && User == "all"` (single mode already cleared `-u`, so `-u all` there warns and falls back to machine columns — pmsd); one `GroupBy(Relabel(raw, Period), Tool, Date, dim)` pass, summing in record input order (the TS float association) — all tools keeps only the groups on the current label, a tool with none gaining no entry (DC-01); a single source uses the first-seen `dim` key set over ALL the tool's raw records (un-windowed), each slice's totals the current-label group's or the zero `fact.Totals{}` — the TS zero-fill, so `tu cc --by-machine --json` emits `machines` with `0` values on a zero-usage day (a G0 candidate against the DC-01 sentence; the harness byte-diff is the bar). Renders by format: `json.Snapshot(rows, bd)`, `csv.Snapshot(rows, bd)`, `markdown.Snapshot(rows, Period, bd)`, or — when `deps.live().Compact` is false — `ansi.Table(view.Snapshot(rows, Period, bd, view.SnapshotOptions{Metric: metric, Prev: deps.live().Prev}), deps.Colors)`, and when it is true (watch mode on a narrow terminal) `ansi.CompactTable(view.CompactSnapshot(rows, Period, metric, deps.live().Prev), deps.Colors)` (4pze) — `bd` nil when the flag is off, byte-identical to the no-breakdown output ([query-view-render](/go-port/query-view-render.md) owns the structure).
4. **History** — computes `recs = query.RollUp(query.Window(query.Collapse(raw, Tool, Date), Since, Until), Period)` (window before roll-up, so a mid-week window yields a leading partial week labeled by its Sunday) and builds registry-ordered `[]view.Series` from ONE `query.GroupBy(recs, query.Tool, query.Date)` pass — each group's `Key.Tool` selects the series, `Key.Date` the entry label; entries stay ascending because `RollUp` sorted and `GroupBy` preserves first-seen order. No per-tool aggregation loop. For a single source under `--by-machine` (the only history path that keeps the flag), `buildHistoryBreakdown` groups the SAME raw records: `GroupBy(Relabel(Window(raw, Since, Until), Period), Tool, Date, dim)` in one pass — `Rows[label]` appends one slice per group in first-seen order, summing in record input order (the TS association — under `-u all`, a user with two machines in one period sums `((a1+a2)+b1)+b2`, never `(a1+a2)+(b1+b2)`) (pmsd). Renders by source count and format with `opts = view.HistoryOptions{Period, Now: deps.Now(), Width: deps.Width, CapActive: capActive, Metric, Prev: deps.live().Prev, MaxRows: deps.live().MaxRows}` (both zero-valued one-shot) (4pze): single source → `view.History(s, opts, bd)` / `json.History(s, bd)` / `csv.History(s, bd)` / `markdown.History(s, period, capActive, bd)`; all tools → `view.TotalHistory(series, opts)` / `json.TotalHistory(series)` / `csv.TotalHistory(series)` / `markdown.TotalHistory(series, period, capActive)`; tables encode through `ansi.Table(…, deps.Colors)`, or — when `deps.live().Compact` and the format is the table — through `ansi.CompactTable(view.CompactHistory(series[0], opts))` / `ansi.CompactTable(view.CompactTotalHistory(series, opts))` (4pze).
5. Returns `Result{Lines, Notices, Warnings, TotalCost, TotalTokens, CostByItem}` — `Notices` are the guard lines from step 1 (the edge writes them before the source warnings); `Warnings` are the source errors the edge writes via `source.WriteWarnings`; `TotalCost`/`TotalTokens` sum the rows (history: every entry) over the UNTRUNCATED data — the watch `MaxRows` window only limits what renders (4pze); `CostByItem` maps display name to cost on the snapshot, and `{Name}:{label}` plus `total:{label}` on history, valued in the display metric (the TS `buildCostMap` scheme).

Caching is uniform: every path goes through `Source{Cache: cache.Default()}` and `--fresh` skips the read everywhere. The TS daily-all cache bypass (bare `tu`/`tu --json` never touch `~/.tu/cache` and `--fresh` is a no-op there) is deliberately NOT reproduced — the cache is not a Goal-listed external surface, the harness cannot observe it, and Constitution Principle IV wants heavy operations cached; it is recorded as a drop-at-cutover candidate for gate G0. (3am6)

### Requirement: Deps.Live carries the per-poll watch render options
`Deps.Live *LiveOptions{Prev map[string]float64, Compact bool, MaxRows int}` is nil for one-shot runs — every path with a nil `Live` is byte-identical to the one-shot output — and set per poll by the watch loop's `Poll` closure, together with the per-poll `Deps.Width` (4pze). They are render state, not CLI grammar — `Request` stays the parse tree. `runSnapshot`, `runHistory`, `runLeaderboard` and `runLeaderboardHistory` pass `Prev` into the view options (the snapshot and leaderboard metric cells' in-cell deltas, the history tables' trailing `Row.Delta`); the snapshot and history table paths render the compact tables when `Live.Compact`; the history paths truncate to `Live.MaxRows`. `Result.TotalCost`/`TotalTokens`/`CostByItem` are computed over the untruncated data as in one-shot (the TS sums before rendering). The full contract of the loop that sets these is [watch-mode](/go-port/watch-mode.md); the render side is [query-view-render](/go-port/query-view-render.md).

### Requirement: The leaderboards are gated to multi mode and served repo-only
`ErrLeaderboardMode = "Error: lb requires multi mode — run tu init-metrics <repo-url> to set up a metrics repo"` — the message names `lb` even for `lbh` (DC-14). `Run` returns it (with an empty `Result` — no notices, no fetch, no lines) when `Display ∈ {Leaderboard, LeaderboardHistory}` and the **post-guard** `cfg.Mode == config.Single`, evaluated BEFORE `Normalize` runs (the TS `main()` order: the `-u` single-mode guard exempts the leaderboards and this guard fires next), so a demoted multi config (the clone guard's fallback) lands on this message after the guard's own stderr lines. `cmd/tu` prints it through its existing non-`ErrUnported` error path, exit 1. Both displays are repo-only (the TS `ALL_USERS` path): records come from `gatherAllUsers(deps.Repo, tools)` — users ascending, tools in registry order per user, walk order within a tool; the harness call log records zero ccusage calls on both sides — with no live fetch, no source warnings (`Result.Warnings` nil) and no writes. `Deps.LastSync func() string` (a closure the edge builds over `config.LastSync`, like `Now`; nil reads as `"never"` in tests) is evaluated only on the `lb` path, so no other command reads `.last-sync` (2gbb).

`leaderboardWindows(period, since, until, now) (cur, prev)` resolves the two ranking windows (the TS `currentWindow`/`previousWindow`): an explicit bound REPLACES the period window — cur label `S → U` / `S →` / `→ U` — and prev is the equal-length range ending the day before `S` (label `prev`; nil for an `--until`-only window, where every row is `new` — DC-13 — and nil when the length is < 1). Period windows anchor on `now` in LOCAL time: daily (today/yesterday), weekly (this Sunday→today / the previous Sunday-anchored week), monthly (`YYYY-MM-01`→today / the previous calendar month via local `time.Date(y, m-1, 1)` / `time.Date(y, m, 0)`, labelled with the English 3-letter month of its last day). Day arithmetic on the ISO bounds is UTC-parsed (`addDays`/`diffDays`) — timezone-independent like `WeekLabel`. A shape-valid but unparseable bound (e.g. `lb --since 2026-13-01`, which passes `Parse`'s shape-only validation) degrades gracefully: `addDays`/`diffDays` return the input/`0`, so the Go side renders the empty leaderboard with exit 0 where the TS `previousWindow` throws a RangeError (uncaught stack, exit 1) — an unmatchable crash path per the expected-diff precedent, recorded as a G0 note; no harness case observes it.

`rankLeaderboard` ranks keys (user, or `user/machine` under `--by-machine`) over the current window (the TS `sumByKey` + `buildLeaderboard`): `daily = SortByDate(Collapse(raw, User[, Machine], Date))` — the per-day collapse sums each key's tools and machines in RECORD INPUT ORDER (the TS `mergeEntries` fold) and `SortByDate` makes the window sum fold ascending by label; the JSON `cost`/`share`/`delta` doubles are byte surfaces (DC-10). One `GroupBy` per window over the same collapsed records (prev map nil when there is no previous window). Keys with `TotalTokens == 0 && TotalCost == 0` in the window are dropped (a key with previous-window data only drops too). Sort descending by the metric value (cost, or tokens under `-t`), ties by key ascending byte order; `grand` folds the metric value over the RANKED rows from 0; `Share = value/grand` (0 when `grand == 0`); `Delta = (value − prevValue)/prevValue` when the previous-window value is nonzero, else nil (rendered `new`, JSON `null`, CSV empty); `Rank = i+1`. `Result.TotalCost`/`TotalTokens` fold over ALL ranked rows; `Result.CostByItem[key] = metricValue` keyed `user` or `user/machine` (the TS `leaderboardPrevMap` — the watch seam) (2gbb).

`runLeaderboard` computes the ranked list in full; `--top n` slices what each format EMITS — shares, the Total row and the `Result` totals always cover the full set — and renders by format: `json.Leaderboard(sliced)` / `csv.Leaderboard(sliced, all, byMachine)` / `markdown.Leaderboard(sliced, all, period, deltaLabel, byMachine)` / `ansi.Table(view.Leaderboard(all, opts))` with `PinnedUser = Flags.User` when set else `cfg.User` (the ` ◂` pin; `-u <name>` never filters), `LastSync = deps.lastSync()`, `Width = deps.Width`. `runLeaderboardHistory` builds one `view.Series` per `Repo.Users()` profile (in that order; an empty series is kept): each entry's value is the LEFT FOLD in record input order over `GroupBy(Relabel(Window(ByUser(raw, u), since, until), period), Date)` — tool-major, walk order within a tool (the TS `aggregateMachineMap` + `sumLeaderboardToolMaps`), DISTINCT from the main history's collapse-then-roll-up association ([multi-mode](/go-port/multi-mode.md)) — entries sorted ascending by label after grouping. `foldColumns(series, Top, metric)` (the TS `foldLeaderboardColumns`) folds under `--top`: a stable descending sort by series total picks the top N, the kept series stay in ORIGINAL order, and one `others` series — the folded entries concatenated in original order, re-grouped per label — is appended LAST; `n ≥ len` folds nothing. Renders through the shared history encoders — `renderjson.TotalHistory` / `csv.TotalHistory` / `markdown.TotalHistory(…, title)` / `ansi.Table(view.TotalHistory(series, opts))` with the `Title`, `RankColumns`, `HighlightLeader` and `KeepAllColumns` hooks set ([query-view-render](/go-port/query-view-render.md)) — with `Result.TotalCost`/`TotalTokens`/`CostByItem` (`{user}:{label}`, `total:{label}`) computed as `runHistory` computes them (the TS `buildCostMap`) (2gbb).

### Requirement: Placeholder policy and the row map
A recognized-but-unported request prints `tu: not implemented (Go port in progress)` on stderr, stdout empty, exit 1. The placeholder's only remaining trigger is `--skip-brew-update` on a data command (the TS silently accepts it — the toolkit flag family, B8's row) (4pze). `runCommand`'s `default` arm remains as defense for nothing the grammar names — only unreachable requests fall through, so it is not dead code. Implemented for real: the single-mode snapshot (3am6), the single-mode history displays, windows, `--full`, the CSV/Markdown encoders and both history JSON shapes (9ax5), the setup commands `init-conf`/`init-metrics`/`status` and the `--dry-run` misuse guard (4fs0), the toolkit surfaces `help`/`-h`/`--help`, `help-dump`, `skill`, `shell-init` (three shells plus both usage errors) and `update` with `--skip-brew-update` (vcur — [toolkit-layer](/go-port/toolkit-layer.md)), multi mode with `-u <user>` / `-u all` and the auto-clone guard (xivf — [multi-mode](/go-port/multi-mode.md)), the `--by-machine` machine/user columns on the snapshots and the single-tool history — both warn-and-clear guards, the dimension switch, the single-source zero-fill, the letter-coded ANSI columns and legend, and the JSON/CSV/Markdown machine shapes (pmsd — [query-view-render](/go-port/query-view-render.md)), the leaderboards `lb`/`lbh` — the exit-1 single-mode gate, the ranking windows and previous-window deltas, `--top` (slice on `lb`, fold on `lbh`), `--by-machine` on `lb`, the `-u` pin/no-op semantics, and all four formats (2gbb), and the sync surfaces — `tu sync` in the TS `runSync` order, `tu sync --dry-run`'s byte-exact report, `--sync` on a multi-mode data command, and the own-user day-file write behind every multi-mode data command (lsml — [metrics-sync](/go-port/metrics-sync.md)), and watch mode — `-w` on every display type with the live `Prev` delta maps in their three placements, the constant 15-row budget, the compact tables under 60 columns, and the `--no-rain`/`--interval` silent acceptance without `-w` (4pze — [watch-mode](/go-port/watch-mode.md)).

### Requirement: cmd/tu run() write order and exit codes
`run(args, stdout, stderr) int` is the only writer — nothing below `cmd/tu` touches stdout/stderr or calls `os.Exit` — in the TS `main()` order: (1) `command.Parse` — a usage error prints the message, then `ShortUsage` when `ShowUsage`, exit `ExitUsage`; (2) `Version` → `toolkit.VersionLine(version)` on stdout, exit `ExitOK` (the version-line helper lives in [toolkit-layer](/go-port/toolkit-layer.md); `var version` and the `-X main.version=` stamp live in `main`); (3) `Command != ""` → `runCommand`: the toolkit commands `help`/`-h`/`--help`, `help-dump`, `skill`, `shell-init` and `update` are answered for real BEFORE path resolution — none of them needs `$HOME` (their sequences, messages and exit codes are in [toolkit-layer](/go-port/toolkit-layer.md)) — and `init-conf`/`init-metrics`/`status`/`sync` are answered for real with paths resolved first (`ErrNoHome` → its byte-exact message, `ExitOperational`; the setup-command handlers and the clone execution are in [config-and-setup](/go-port/config-and-setup.md); `sync` runs the TS `runSync` order — `config.Load` warnings, the reserved-user guard exit 2 BEFORE the mode check, the two-line `tu sync requires metrics_repo to be set.` gate exit 1, `MetricsDirGuard` with a demotion exiting 1 with nothing more, then the byte-exact `Report.Format` dry-run on stdout or the live sync's `Synced to {Tildefy(MetricsDir)}` on stdout (`!OK` → `Error: sync failed — check network and remote config.`, exit 1) — in [metrics-sync](/go-port/metrics-sync.md) (lsml)); every other command → placeholder, `ExitOperational`; (4) `config.ResolvePaths(os.Getenv("HOME"))` error → its message, `ExitOperational` (parse precedes the HOME check: `tu bogus` with HOME unset still exits 2); (5) `config.Load(paths, env, Overrides{})` — its cascade warnings print on stderr before anything else; (5a) `cfg, guardLines := config.MetricsDirGuard(cfg, config.StateDir(paths.Home), time.Now(), metricsync.Exec{})` — the guard's lines print to stderr in order (data path only: setup commands never clone here — `status` only reads, `init-metrics` has its own interactive clone); (6) the reserved-user guard on the post-guard `cfg.User` — `cfg.User == "all"` → `Error: config user "all" is reserved (used by -u all)` on stderr, `ExitUsage`; (7) deps — `&ccusage.Source{Cache: cache.Default(), User: cfg.User, Machine: cfg.Machine}` (live records stamped from config — the own/other split and `MaxMerge` key on that identity), `Repo: metrics.Source{Dir: cfg.MetricsDir}`, `Writer: metricsync.Writer{Dir: cfg.MetricsDir}` (the own-user day-file write `gatherOwn` runs per tool before each repo read — lsml; the compile-time `Fetcher`/`Repo`/`Writer`/`metricsync.Fetcher` assertions sit beside the assignment; only `cmd/tu` names `ccusage` and `metrics`), `Now: time.Now`, `ansi.Colors{Enabled: !NoColor && os.Getenv("NO_COLOR") == ""}`, `Width: terminalWidth(stdout)` — `terminalWidth` returns `term.GetSize`'s column count when stdout is an `*os.File` whose descriptor `term.IsTerminal` reports a TTY (via `golang.org/x/term`), else **80** (a pipe, a `bytes.Buffer` in tests, or a probe error); `COLUMNS` is never consulted (DC-12); `LastSync: func() string { return config.LastSync(config.StateDir(paths.Home), time.Now()) }` — a closure like `Now`, evaluated only on the `lb` path so no other command reads the file (2gbb); (7a) the `--sync` block, when `req.Flags.Sync && cfg.Mode == config.Multi` (it sits after the reserved-user guard and before `command.Run`, so its lines precede `Run`'s notices on stderr — the TS `main()` order): `syncing metrics... ` (no newline) to stderr; `metricsync.FullSync(ctx, Inputs{cfg, StateDir(paths.Home), time.Now(), src, metricsync.Exec{}}, false)` under `source.DefaultTimeout` with the SAME `src` the data path uses — the sync's fetch warms the shared cache, so `tu --sync` on a cold cache makes six ccusage calls, not twelve; a filesystem error prints `err.Error()` and exits 1 (the unmatchable TS crash path); else `source.WriteWarnings(stderr, out.Warnings)`, the `out.Lines`, then `synced.` when `out.OK` else `sync failed — using local data.`; in single mode `--sync` produces no output and no git call (lsml); (8) `command.Run(ctx, req, cfg, deps)` — `ErrUnported` → placeholder exit 1, any other error → its text on stderr exit 1; (9) each of `res.Notices` via `fmt.Fprintln(stderr, n)` (the guard warnings, before any fetch warning — the TS `main()` order), then `source.WriteWarnings(stderr, res.Warnings)`, then each line via `fmt.Fprintln(stdout, l)`, exit 0. The edge's `env` is `config.Env{Getenv: os.Getenv, Hostname: os.Hostname, Username: currentUsername}` (`os/user` `Current()`). `main.go` imports `_ "time/tzdata"` so `TZ=Asia/Kolkata` (the harness `tz: alt` axis) resolves on hosts without `/usr/share/zoneinfo`; Go consults the embed only when the system database is missing (~450 KB, no startup cost). (4fs0, vcur)

### Requirement: The watch branch
When `req.Flags.Watch`, after the reserved-user guard and the `--sync` block and before the one-shot `command.Run`, `runWatchBranch` runs the TS `main()` watch dispatch (4pze — the loop itself is [watch-mode](/go-port/watch-mode.md)):

1. `command.Normalize(req, cfg.Mode, time.Now())` runs ONCE at the edge and its notice lines print to stderr BEFORE the alt screen (the TS prints the guards in `main()` ahead of `runWatch`); per-poll `Result.Notices` are discarded (`Normalize` does not clear `--full`, so its notice would repeat every poll). The polls run the ORIGINAL request — `Run` normalizes per poll.
2. The leaderboard gate fires before the alt screen exactly as on the one-shot path: `lb`/`lbh` in single mode → the `ErrLeaderboardMode` line on stderr, exit 1, no alt screen.
3. `watch.Options{Interval, NoRain, Poll, Term, Stderr, Colors, Now: time.Now, Rand: rand.New(rand.NewPCG(seed, ^seed))}` (the seed from `time.Now().UnixNano()`) runs through the two test seams `newWatchTerminal` (production: `watch.NewTerminal(os.Stdout, os.Stdin)`) and `runWatchLoop` (production: `watch.Run`). The `Poll` closure copies `deps`, sets `Width: f.Width` and `Live: &command.LiveOptions{Prev: f.Prev, Compact: f.Compact, MaxRows: f.MaxRows}` — for the leaderboards a nil `f.Prev` becomes an EMPTY non-nil map (the TS passes the live `_lastRenderCostMap`, so the indicator column is reserved from the first frame) — sets `Flags.Fresh = true` (the TS `action(true, …)`: every poll bypasses the 60 s cache), calls `command.Run`, writes `source.WriteWarnings(stderr, res.Warnings)` every poll (the TS fetcher warns on stderr, alt screen or not), and returns the lines plus `watch.Stats{TotalCost, TotalTokens, CostByItem}`.
4. After the loop returns, each of the last rendered lines prints to stdout + `\n` and the exit code is 0.

#### Scenario: Notice once, before the alt screen
- **GIVEN** `tu -u bob -w` in single mode
- **WHEN** the binary starts
- **THEN** stderr receives the `-u` notice once, before `\x1b[?1049h`, and no notice repeats on later polls

### Requirement: End-to-end test against the fake ccusage
`src/go/cmd/tu/e2e_test.go` runs `run` against `TestMain`-built `fakeccusage` and `fakegit` (as `git`, first on PATH) replaying the `_placeholder` corpus only (`TUDIFF_FIXTURES`), with staged `$HOME`s (`harness.StageHome` for the four conf variants against the committed metrics-repo seed), `TZ=UTC`, `NO_COLOR` and `TU_METRICS_REPO` unset. It asserts byte-exact stdout/stderr/exit for the empty-state table (`tu`, `tu cc`, `tu m`, `tu w`, `tu --no-color`), the all-zero JSON (`tu --json`), the `tu bogus` usage error, `status` in each of the four staged variants, `init-conf` create/copy/complete, `init-metrics` unset/already-initialized/clone (asserting the recorded git argv via `TUDIFF_CALL_LOG`), `init-metrics a b`, `tu --dry-run`, a legacy-conf snapshot (deprecation line then the empty table, exit 0), `tu cc` with `user = all` (exit 2), the three setup commands with HOME unset (exit 1), and the history surfaces: `cc h --since 2026-01-01 --until 2026-01-31` (populated bytes, exit 0), `h` (capped heading + `  No data`), `mh` (one row, no Total), `wh --since … --until …` (the `2026-01-04` partial-week row), `h --json`, `cc mh --csv`, `cc mh --md`, the `--csv`/`--md` snapshot empties, `--since 2026-13-01` (stderr warning + empty snapshot, exit 0), and the by-machine empty states: `h --by-machine` (the pivot warning on stderr, then the capped pivot heading + `  No data`, exit 0) and `cc h --by-machine` (the capped single-tool heading + `  No data`, no notice). The multi-mode surface: staged `multi`/`org`/`legacy` homes running `cc h --since 2026-01-01 --until 2026-01-31` against the seed (the populated merged bytes — `$0.50` / `$1.15` / `$0.50`, Total `$2.15`) and `cc mh --json` (`"totalCost": 2.15` with the pinned token counts), `-u other-user` and `-u all` (empty snapshot, **no** ccusage call in `TUDIFF_CALL_LOG`), `-u other-user` in a single home (the warn-and-clear line, six ccusage calls, exit 0), the `envrepo`-style clone path (`TU_METRICS_REPO` set on a single home: git argv `clone <url> <abs dir>` asserted, the `Cloned metrics repo → …` stderr line), and a fresh `.clone-failed` marker (the `not available` warning, no git call, exit 0). The multi-home by-machine surface: `--by-machine -u all` on today's snapshot (`  No usage`, no columns, no legend), the populated window `cc h --by-machine --since 2026-01-01 --until 2026-01-31` (columns `A` = harness-machine, `B` = other-box; the `2026-01-06` row splits `$0.75`/`$0.40`; Total `$1.75`/`$0.40`; the dim legend `Machines: A = harness-machine, B = other-box` after the footer), `cc mh --by-machine --json` (the `machines` object in first-seen order — `harness-machine` before `other-box`), `cc mh --by-machine -u all --csv` (user-keyed columns, the `machine_` header prefix kept), and the pinned-machine single-home `cc h --by-machine -u all --since …` (the `-u` warning, one machine column `A = harness-machine`). The e2e binary sees a `bytes.Buffer`, so width is 80 throughout and bars never render. Remaining populated-path coverage lives in the render goldens and the fake-`Fetcher`/fake-`Repo` `Run` tests with a fixed `Now` and injected `Width` — including the breakdown's dimension switch, single-source zero-fill key set, JSON key order, and `-u all` float association (see [query-view-render](/go-port/query-view-render.md)). The leaderboard surface: single-home `lb`, `lbh` and `lb -u other-user` → the `lb requires multi mode` line on stderr, empty stdout, exit 1, and NO `-u` notice; multi-home bare `lb` (today's empty window — the placeholder corpus predates "today" — plus the `never synced · tu sync to refresh` footer), `lb -u all` (no notice), `lb --full` (the warn line, still the empty table), and `--top 3` on a snapshot (the byte-exact `--top` warn-and-ignore line, exit 0); the populated January window `lb --since 2026-01-01 --until 2026-01-31` (rows `1 | harness-user ◂ | $1.70 | 97,600 | 56.7% | new`, `2 | other-user | $1.30 | 48,800 | 43.3% | new`; Total `$3.00` / `146,400`; the two-user table is narrow enough that bars render at 80 columns) in table, JSON (raw `1.7`/`1.3` doubles, `"delta": null`), CSV (`0.567`/`0.433` shares, empty delta fields, the `Total,,3.00,146400,,` row), and Markdown; `cc lb` on the window (`$1.40` / `$1.10`); `--by-machine` (`other-user/laptop`, `harness-user/harness-machine ◂`, `harness-user/other-box ◂`) and `--by-machine -t` (the three-way 48,800-token tie broken by key name); `--top 1` (the dim `… +1 others` line, Total still `$3.00`); `-t` (re-ranked by tokens); `-u other-user` (the pin moves to that user's row); `lb --until 2026-01-31` (heading `· → 2026-01-31 ·`, every row `new`); a staged `.last-sync` file (footer `synced 15m ago (…) · tu sync to refresh` with a fixed `Now`); and the `lbh` surfaces — `m lbh` (one `2026-01` row, columns ranked `harness-user $1.70` before `other-user $1.30`, the bold leader cell, stacked bars, no Total for a single label), `--json` (users in `Repo.Users()` order), `--csv`, `--md` (`## Leaderboard History (monthly)`), `m lbh --top 1` (the `others` column; JSON keys `harness-user`, `others` in that order), `-t`, `lbh --since … --until …` (daily rows with the month-separator rules), `lbh --full` (same bytes as the explicit window), and `lbh --by-machine` (the warning line, then the capped `📊 Leaderboard History (daily, last 3 months)` + `  No data`). The sync surface (lsml): `sync` on the single home (the two-line error, exit 1, no git call), on the multi/org/legacy homes (`Synced to ~/.tu/metrics_repo`, exit 0, `.last-sync` created, the day-files written — `cc-2026-01-05.jsonl` holds `0.5`, `cc-2026-01-06.jsonl` still `0.75`, `cc-2026-01-07.jsonl` new — and the git argv sequence asserted from `TUDIFF_CALL_LOG`); `sync --dry-run` on multi (the byte-exact report captured from `node dist/tu.mjs` on an identically staged home, no file created, no `.last-sync`, exactly one git call); `sync` with `user = all` (exit 2); a fresh `.clone-failed` marker (the `not available` line, exit 1, no git call); the `TUDIFF_GIT_SCRIPT` `pullfail`/`pushfail` variants (exact stderr, exit 1, `rebase --abort` after the failed pull) and the `dirty` variant's `commit -m "# harness-user: update {UTC date}"` argv; `cc --sync` (`syncing metrics... synced.\n` then the table, six ccusage calls total — the second fetch hits the cache the sync warmed); `--sync` on the single home (no sync line); `sync --json` (data flags on `sync` ignored, DC-02); and a multi-home `cc h --since … --until …` that leaves the written day-files behind while rendering byte-identical output. (4fs0, 9ax5, pmsd, 2gbb, lsml)

### Requirement: Harness gate status
`just go-diff --placeholder` reports 452 of 452 cases green — by conf `single 182/182`, `multi 176/176`, `org 47/47`, `legacy 47/47`; by env `default 370/370`, `nocolor 34/34`, `envrepo 32/32`, `pullfail 8/8`, `pushfail 4/4`, `dirty 4/4`; by io `pipe 376/376`, `tty 76/76`; by tz `fixed 385/385`, `alt 67/67` — with no `tree`-channel red and no `[calls differ]` on any sync group. The green set: the single-mode snapshot and history cases (base, `-window` under both time zones, `-full`, the json/csv/md format cases), the `since-invalid` warn-and-render case, the usage-error groups, the watch family (4pze) — the exit-2 incompatibilities `watch-json`/`watch-csv`/`watch-md`, the interval validation cases `watch-interval-min` (`-w -i 3`) / `watch-interval-max` (`-w -i 4000`) / `watch-interval-nan` (`-w -i abc`), and the DC-03 silent-acceptance cases `no-rain-without-watch` (conf single/multi × io pipe/tty) and `interval-without-watch-long` (`--interval 30`), the 20 setup-command cases (`status`/`init-conf`/`init-metrics`/`init-metrics-url`/`init-metrics-extra` × the four conf variants), the two `--dry-run` misuse cases, the ten toolkit cases (`help` pipe and tty, `help-cmd`, `help-dump`, `skill`, `shell-init-bash`, `shell-init-zsh`, `shell-init-fish`, `shell-init-missing`, `shell-init-unknown`), the multi-mode cases — the `multi`/`org`/`legacy` snapshot and history cases across the `default`/`nocolor`/`envrepo` envs (including the **populated** own-user merge: `cc 2026-01-06` renders `$1.15` and the cc month `$2.15` against the seed), the per-tool multi snapshots, the multi-mode format cases, the `-window`/`-full` history cases under multi, `user-other`/`user-all` in both confs, and the `envrepo` clone cases — every `*by-machine*` case (28: the ten original flag cases plus the nine populated groups × `single`/`multi` — the windowed and monthly single-tool history in table/json/csv/md, token mode, `-u all` on both, and the `snapshot-cc-by-machine-json` zero-fill pin), every leaderboard case: the 44 baseline `lb*`/`*-lb*`/`lbh*` cases (the exit-1 gate on the single/legacy/org confs, the empty state on multi) plus the 34 cases from the 28 added groups — the **populated** January window in table/json/csv/md, `--by-machine` (including the `-t` three-way tie), `--top 1`, `-t`, `-u other-user`, `--until`-only/`--since`-only, `cc lb`, the `m lbh` family (ranked columns, `--top 1` fold, tokens), `lbh --full` under both time zones, the `top-snapshot` warn-and-ignore case, and the `io: tty` `lb-tty` case that exercises the mid-row bar on a real 120-column terminal (2gbb) — and every sync case: the six groups `sync-cmd` (four confs × `default`/`pullfail`/`pushfail`/`dirty`), `sync-dry-run` (four confs × both time zones — `tz: alt` is the DC-21 byte check, a UTC commit date beside local day-file dates), `sync-flag` and `cc-sync` (`single`/`multi` × `default`/`pullfail`), `cc-sync-json`, and `sync-json` (a real sync with data flags ignored, DC-02), each also proving the written day-file tree and `.last-sync` presence through the `tree` channel (lsml). Caveats: the placeholder corpus's dates (2026-01-05..07) never match "today", so snapshot cases exercise only the empty table and the all-zero JSON; bars on the snapshot/history tables (80 columns never leaves room there), column omission and dim zero cells (equal nonzero costs), month separators (all January), the current-period marker and weekend dimming, and token mode on the single-tool history are pinned by goldens only — the leaderboard's bars, month separators (`lbh --since/--until`) and leader highlight have harness (`lb-tty`) and e2e coverage; the local-capture harness run is the real-bytes check for the rest. See [differential-harness](/harness/differential-harness.md).

## Design Decisions

### Reproduce the JSON label quirk, not the cache quirk
**Decision**: `Run` clears labels on the single-mode daily-all path; `Run` caches on every path.
**Why**: The label omission is a harness-compared byte surface; the cache bypass is not, and Principle IV wants heavy work cached. Both are flagged for gate G0 (a spec correction / DC candidate for the label; a drop-at-cutover candidate for the cache).
**Rejected**: Reproducing both (a faithful port of a non-surface quirk against the constitution); reproducing neither (a harness red on populated data).
*Introduced by*: 260916-3am6-query-view-render-snapshot

### The full parser lands in one row
**Decision**: `Parse` implements the complete TS grammar and validation — every flag, every byte-exact message, the TS check order, version after validation, the non-data tokens — even though most parsed requests route to the placeholder.
**Why**: The parser is a single function in the TS; splitting it across rows would have the config/history/sync rows re-touch the same validation table, and the byte-exact usage errors are themselves harness cases that go green from the complete parser.
**Rejected**: Parsing only the snapshot grammar and growing it per row.
*Introduced by*: 260916-3am6-query-view-render-snapshot

### Placeholder for recognized-but-unported requests
**Decision**: `Run` returns `ErrUnported` for requests later rows own; `cmd/tu` prints the scaffold's unchanged placeholder line with exit 1.
**Why**: Unported harness cases stay red with the signature the harness already knows, and `Unknown argument` fires only where the TS fires it.
**Rejected**: Treating unported flags as unknown arguments (a false divergence on the error text).
*Introduced by*: 260916-3am6-query-view-render-snapshot

### Flag guards are a pure Normalize step whose notices ride the Result
**Decision**: `command.Normalize` clears/defaults flags and returns notice lines; `cmd/tu` prints them before the source warnings.
**Why**: Nothing below `cmd/tu` may write; the TS prints these before any fetch, so ordering notices ahead of `WriteWarnings` reproduces the stderr byte order; `since-invalid` must be in scope after the clear.
**Rejected**: Printing from `command` (breaks the only-writer rule); treating the guarded flags as out of scope (turns a warn-and-render exit 0 into the placeholder's exit 1).
*Introduced by*: 260916-9ax5-history-and-periods

### A Repo interface beside Fetcher, not a second Fetcher
**Decision**: `command` consumes the metrics clone through `Repo{Users, Read}`; `metrics.Source` implements it, asserted at the `cmd/tu` assignment.
**Why**: A repo read has no context, period, extra args, cache, fresh flag or error channel — forcing it through `Fetcher` would mean ignored parameters and a fake `*source.Error`. Two small honest interfaces keep `command` adapter-free and give the later rows `Users()` and per-user reads directly.
**Rejected**: `metrics.Source` satisfying `Fetcher` (run.go's original comment) — ignored parameters and no way to express "all users" or "one user" without smuggling them through `extraArgs`.
*Introduced by*: 260916-xivf-metrics-source-and-multi-mode

### Terminal width via golang.org/x/term at the edge
**Decision**: `cmd/tu` probes stdout with `x/term` (`IsTerminal` + `GetSize` on the descriptor) and hands `Deps.Width` (80 when not a TTY) down; render never probes, and `COLUMNS` is never read.
**Why**: DC-12 fixes the 80-column pipe default and forbids `COLUMNS`; the plan's watch mode builds on `x/term` and the six sibling tools depend on it; a stdlib ioctl needs per-OS build tags for five lines.
**Rejected**: Hardcoding 80 until B7 (bars invisible in a real terminal during dogfood prep); a stdlib `TIOCGWINSZ` probe (build-tag surface for no gain).
*Introduced by*: 260916-9ax5-history-and-periods
