# Migration: 0.39.0 to 0.40.0

## Summary

Remove the `source fab/.kit/scripts/lib/env-packages.sh` line from `.envrc`. This script was redundant — `.envrc` already has `PATH_add fab/.kit/bin` which does the same thing. The script has been deleted.

## Pre-check

1. Confirm `fab/.kit/VERSION` reads `0.40.0` or higher (kit has been upgraded)
2. Check if `.envrc` contains the line `source fab/.kit/scripts/lib/env-packages.sh` — if not, skip with a note

## Changes

### 1. Remove `env-packages.sh` source line from `.envrc`

If `.envrc` contains a line matching `source fab/.kit/scripts/lib/env-packages.sh`:

1. Remove that line from `.envrc`
2. Print a note:
   ```
   Removed 'source fab/.kit/scripts/lib/env-packages.sh' from .envrc (redundant with PATH_add fab/.kit/bin).
   ```

If `.envrc` does not contain that line:

1. Print a note:
   ```
   .envrc already clean, skipping.
   ```

## Verification

1. `.envrc` should not contain `env-packages.sh`
2. `.envrc` should still contain `PATH_add fab/.kit/bin`
