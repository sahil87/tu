Verdict: GO

# G1 architecture review — cycle 2

Reviewed at `origin/main` a6f0d78 (V1 #84, V2 #85, B1 #86, B2 #87, rework #88)
on 2026-09-17 by the sunny-turtle session, per
`fab/plans/sahil/26-09-15-go-port.md` § G1 review protocol. Mechanical checks
on this tree: `gofmt -l` empty, `go vet ./...` clean, `go test ./... -count=1`
all packages ok, `just go-diff --placeholder` 134/368 green with per-case
verdicts identical to cycle 1 (no case changed state in either direction).

## Cycle-1 rework findings — resolved

1. **Registry coupling.** `internal/command` now imports `config`, `fact`,
   `query`, `render/*`, `source`, `view` and no adapter. `Fetcher.Fetch` takes
   `fact.Tool`; `fact.Tools`/`fact.Lookup` own key, display name and column
   order (`internal/fact/tool.go`); `source.PeriodDaily` and
   `source.DefaultTimeout` moved to `internal/source/fetch.go`; the ccusage
   adapter keeps `PrefixArgs`/`LabelKey` privately keyed by `fact` key. The
   only non-test mention of `ccusage` outside the adapter is the constructor
   in `cmd/tu/main.go`. Resolved as specified.
2. **`view.TotalHistory` size.** Split into `pivotValues`, `pivotData`,
   `pivotWidths`, `pivotBodyWidth`, `pivotColumns`, `pivotRows`,
   `pivotTotals`; `History` mirrored with `historyValues`, `historyColumns`,
   `historyRows` and a shared `barBudget`. No function in `internal/view`
   exceeds 50 lines. Resolved as specified.

## Follow-ups (carried forward, not rework)

Findings 3, 4 and 5 from `g1-1.md` stand unchanged: `view` importing `render`
for number formatting; Go test volume; documenting the two-model split for
JSON/CSV vs table encoders before B5. One addition:

6. **Helper parameter lists in `view/pivot.go`.** `pivotRows` and
   `pivotWidths` take six to eight positional parameters. Fine for now; if
   B4/B5 need to add more, introduce a `pivotState` struct rather than a
   ninth parameter.

Remaining functions over 50 lines (`command.Parse` 89, `scanFlags` 85,
`validate` 71, `config.Load` 69, `command.runHistory` 67,
`markdown.TotalHistory` 67, `cmd/tu.run` 67) are flat sequences or flag tables
and were explicitly left alone in cycle 1.

## Checklist record

| # | Item | Result |
|---|------|--------|
| 1 | Package boundaries / I/O isolation | ok — cycle-1 findings 1 and 2 resolved; follow-up 3 open |
| 2 | One group-by | ok |
| 3 | No result globals | ok |
| 4 | Typed errors at the edge | ok |
| 5 | Test shape | ok — follow-up 4 open |
| 6 | Harness burndown | ok — 134/368, identical case verdicts to cycle 1; every red single-mode case still explained by an unported row |
| 7 | Constitution / unshipped | ok — #88 touched only `src/go/`, `docs/memory/go-port/`, `fab/changes/` |
| 8 | gofmt / vet / test | ok |

The queue may continue with B8.
